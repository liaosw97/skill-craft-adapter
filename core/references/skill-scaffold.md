---
name: {skill-name}
description: |
  {2-3 句描述：做什么 + 触发场景 + 输出什么}
  触发：{触发条件摘要}
  DO NOT TRIGGER：{不触发场景}
tools:
  - Read
  - Grep
  - Glob
  - Bash
  # - Edit    # 如需修改文件
  # - Write   # 如需创建文件
---

# {Skill 标题}

## 触发条件

<!-- 模块 1 — 必需 -->

触发（同时满足）:
- 用户提供了 {具体输入物类型}
- 用户意图是 {执行/分析/生成}（非讨论/提问/引用）

不触发:
- {场景1}
- {场景2}

歧义处理:
- 无法判断时询问: "{具体澄清问题}"

---

## 行为准则

<!-- 模块 2 — 必需 -->

以下规则在整个会话期间有效，不因对话长度而放松：

1. ❗ {最关键规则} — 每次输出前自检
2. ❗ {第二关键规则} — 每次输出前自检
3. {其他规则}

---

## 工具优先级

<!-- 模块 3 — 中等/重型必需 -->
<!-- 轻量 Skill 可省略此模块 -->

| 操作 | 首选工具 | 降级条件 | 降级工具 |
|------|---------|---------|---------|
| {操作A} | {专用工具} | 连续 2 次失败 | {通用替代} |
| {操作B} | {专用工具} | Step X 确认不可用 | {通用替代} |

- 单次超时 ≠ 工具不可用，必须重试 1 次
- 降级必须标注: "⚠️ 降级: [原因]"

---

## 输出约束

<!-- 模块 4 — 必需 -->

禁止输出:
- "让我来分析一下..." 等开场白
- 工具调用前后的重复描述
- 已知信息的复述
- {领域相关的冗余模式}

产物约束:
- {具体限制}

---

## 执行流程

<!-- 模块 5 — 中等/重型必需 -->

### Step 1: {动作}

{操作指令}

完成性要求:
- 输入集: {来源和数量}
- 必须处理: {每个/全部/≥N%}
- 验收条件: 已处理数 == 输入总数

失败降级:
- {操作A} 失败 → {操作B} → B 也失败 → 记录原因，不允许跳过

✅ Checkpoint: `Step 1 完成: {量化指标}`

### Step 2: {动作}

{操作指令}

✅ Checkpoint: `Step 2 完成: {量化指标}`

---

## 依赖链

<!-- 模块 6 — 重型必需（多步骤且后步依赖前步时） -->
<!-- 轻量/中等 Skill 可省略 -->

Step 2 的输入 = Step 1 的完整产出（不可重新搜索）

Checkpoint 传递:
- Step 1: "完成: N 项"
- Step 2: "输入 N 项（来自 Step 1），新增 M 项"

交叉验证（写入文件前）:
- [ ] 产物中的数字 == 各步骤 Checkpoint 累计值

---

## Decision Gate

<!-- 输出判断、建议、评分、改动或风险结论时必需 -->
<!-- 不输出强结论的轻量 Skill 可标 N/A -->

Signal candidates:
- {只能作为调查起点的信号/关键词/结构命中}

Required evidence by claim type:
- structural: {文件/字段/格式/schema 证据}
- behavioral: {运行结果/测试/日志/复现步骤}
- relational: {引用链/调用链/依赖链/映射关系}
- semantic: {冲突双方原文/术语定义/source of truth}

Counter-evidence checks:
- {例外/缓解条件/版本限制/反例/已有实现/用户约束}

Completeness ceiling:
- complete evidence + checked counter-evidence → accepted
- partial evidence → tentative
- unresolved evidence → unresolved

Decision rules:
- Signal without evidence → unresolved
- Strong decisions must cite evidence and checked counter-evidence

---

## 子 Agent 委派

<!-- 模块 7 — 重型必需（使用子 agent 时） -->
<!-- 轻量/中等 Skill 可省略 -->

委派 prompt 必须包含:
1. 完整约束（复制原文，不转述）
2. 完成性: "输入集共 N 项，必须全部处理"
3. 结果格式: "Checkpoint: 已处理 X/N 项"

并行规则:
- 有数据依赖 → 不可并行
- 边界: "你只负责 X，不涉及 Y"

合并检查:
- [ ] 去重 + 一致性 + 计数验证

---

## 事实性约束

<!-- 模块 8 — 必需 -->

所有结论必须引用来源（工具名+数据 / 文件路径+行号）。
无来源 = 不允许输出。

| 场景 | 正确输出 | 禁止输出 |
|------|---------|---------|
| 搜索返回 0 条 | "PASS: 未发现" | 编造发现 |
| 工具不可用 | "⚠️ 未检测" | 编造分析 |
| 数据不足 | "需进一步验证" | 确定性结论 |

标注:
- 工具确认 → 无标注
- 降级推测 → "⚠️ 降级分析"
- 领域建议 → "💡 通用建议"
